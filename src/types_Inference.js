"use strict";
// type inference
let mensagem = "oi amanda"; // nao preciso declarar o tipo
// Evitar de ficar tipando desnecessáriamente
window.addEventListener("click", (e) => {
    console.log(e.target);
});
